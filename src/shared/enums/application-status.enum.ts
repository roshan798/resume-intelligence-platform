export enum ApplicationStatus {
    SAVED = "saved",
    APPLIED = "applied",
    OA = "OA",
    INTERVIEW = "interview",
    REJECTED = "rejected",
    OFFER = "offer",
    CLOSED = "closed",
}
